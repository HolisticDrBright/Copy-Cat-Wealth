{
  "name": "copy-cat-wealth",
  "private": true,
  "workspaces": [
    "apps/mobile",
    "apps/api",
    "packages/shared"
  ],
  "scripts": {
    "mobile": "npm run start --workspace=apps/mobile",
    "api": "npm run dev --workspace=apps/api",
    "api:build": "npm run build --workspace=apps/api",
    "typecheck": "tsc --noEmit --workspace=packages/shared"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
